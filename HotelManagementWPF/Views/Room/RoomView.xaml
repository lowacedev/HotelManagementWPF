<UserControl x:Class="HotelManagementWPF.Views.Room.RoomView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:HotelManagementWPF.ViewModels"
          
             Background="White">

    <UserControl.Resources>
        <Style x:Key="PillButton" TargetType="Button">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#E0E0E0" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,6" />
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="Foreground" Value="#2C3E50" />
        </Style>

        <Style x:Key="AddButton" TargetType="Button">
            <Setter Property="Background" Value="#2C3E50" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Padding" Value="14,6" />
            <Setter Property="Margin" Value="8,0,0,0" />
        </Style>

        <Style x:Key="PageButton" TargetType="Button">
            <Setter Property="Width" Value="36" />
            <Setter Property="Height" Value="36" />
            <Setter Property="Margin" Value="4,0" />
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#E0E0E0" />
            <Setter Property="BorderThickness" Value="1" />
        </Style>

        <Style x:Key="CleanDataGridRow" TargetType="DataGridRow">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
            <Setter Property="BorderBrush" Value="#F0F0F0" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="Padding" Value="0,8" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F8F9FA" />
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#E3F2FD" />
                </Trigger>
            </Style.Triggers>
        </Style>

        
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header with filters and Add button -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Grid.Column="0">
                <Button Style="{StaticResource PillButton}" Command="{Binding FilterCommand}" CommandParameter="All">
                    <TextBlock Text="{Binding TotalRooms, StringFormat=All Rooms ({0})}" />
                </Button>
                <Button Style="{StaticResource PillButton}" Command="{Binding FilterCommand}" CommandParameter="Available">
                    <TextBlock Text="{Binding AvailableRooms, StringFormat=Available ({0})}" />
                </Button>
                <Button Style="{StaticResource PillButton}" Command="{Binding FilterCommand}" CommandParameter="Booked">
                    <TextBlock Text="{Binding BookedRooms, StringFormat=Booked ({0})}" />
                </Button>
            </StackPanel>

            <Button Grid.Column="1" Style="{StaticResource AddButton}" Command="{Binding AddRoomCommand}" HorizontalAlignment="Right">
                <TextBlock Text="Add Room" />
            </Button>
        </Grid>

        <!-- DataGrid -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding PaginatedRooms}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  RowHeaderWidth="0"
                  SelectionMode="Single"
                  HeadersVisibility="Column"
                  Margin="0,0,0,12"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="#F0F0F0"
                  BorderThickness="0"
                  Background="White"
                  RowBackground="White"
                  AlternatingRowBackground="White"
                  RowStyle="{StaticResource CleanDataGridRow}">
    
            <DataGrid.Columns>
                <DataGridTextColumn Header="Room Number" Binding="{Binding RoomNumber}" Width="2*" />
                <DataGridTextColumn Header="Bed Type" Binding="{Binding BedType}" Width="2*" />
                <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat=C}" Width="*" />
                <DataGridTemplateColumn Header="Status" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border CornerRadius="12" Padding="6,4" Background="{Binding Status, Converter={StaticResource StatusToBackgroundConverter}}" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Status}" Foreground="{Binding Status, Converter={StaticResource StatusToForegroundConverter}}" FontWeight="SemiBold" FontSize="11" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Pagination -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,8,0,0">
            <Button Style="{StaticResource PageButton}" Command="{Binding PreviousPageCommand}">
                <TextBlock Text="Previous" />
            </Button>

            <ItemsControl ItemsSource="{Binding PageNumbers}" Margin="8,0">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Style="{StaticResource PageButton}" Command="{Binding DataContext.GoToPageCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}">
                            <TextBlock Text="{Binding}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Button Style="{StaticResource PageButton}" Command="{Binding NextPageCommand}">
                <TextBlock Text="Next" />
            </Button>
        </StackPanel>
    </Grid>
</UserControl>