<UserControl x:Class="HotelManagementWPF.Views.Booking.BookingView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
        xmlns:local="clr-namespace:HotelManagementWPF.Views.Booking"
        mc:Ignorable="d"
        d:DesignHeight="700" d:DesignWidth="1100"
        Background="#FAFAFA">
    <UserControl.Resources>
        <!-- Color resources -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="#2196F3"/>
        <SolidColorBrush x:Key="SecondaryBrush" Color="#FFA726"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#4CAF50"/>
        <SolidColorBrush x:Key="WarningBrush" Color="#FF5722"/>
        <SolidColorBrush x:Key="BackgroundBrush" Color="#FAFAFA"/>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#FFFFFF"/>
        <Style x:Key="HeaderText" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
        </Style>
        <Style x:Key="SubHeaderText" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Regular"/>
            <Setter Property="Foreground" Value="#7B8A8B"/>
        </Style>
        <Style x:Key="HeaderButton" TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        <Style x:Key="SubHeaderButton" TargetType="Button">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Regular"/>
            <Setter Property="Foreground" Value="#7B8A8B"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
    </UserControl.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- Header -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" VerticalAlignment="Top" Margin="0,0,0,8">
            <TextBlock Text="Reservations" Style="{StaticResource HeaderText}"/>
            <TextBlock Text="  " Width="16"/>
            <TextBlock Text="Manage room" Style="{StaticResource SubHeaderText}" VerticalAlignment="Bottom"/>
        </StackPanel>
        <!-- Navigation & Search -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="&#x276E;" Width="32" Height="32" Margin="0,0,8,0"/>
                <TextBlock Text="Feb 9 - Feb 15, 2025" FontWeight="SemiBold" FontSize="14" VerticalAlignment="Center"/>
                <Button Content="&#x276F;" Width="32" Height="32" Margin="8,0,0,0"/>
                <StackPanel Orientation="Horizontal" Margin="24,0,0,0">
                <Button Content="Day" Style="{StaticResource SubHeaderButton}" Margin="0,0,8,0"/>
                <Button Content="Week" Style="{StaticResource HeaderButton}" Background="#E3F0FF" Margin="0,0,8,0"/>
                <Button Content="Month" Style="{StaticResource SubHeaderButton}"/>
            </StackPanel>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Grid.Column="1" VerticalAlignment="Center"/>
        </Grid>
        <!-- Scheduler -->
        <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="0">
            <syncfusion:SfScheduler x:Name="BookingScheduler"
                                    ViewType="TimelineMonth"
                                    ResourceGroupType="Resource"
                                    ItemsSource="{Binding Bookings}"
                                    ResourceCollection="{Binding Rooms}"
                                    AppointmentMapping="{Binding AppointmentMapping}"
                                    ResourceMapping="{Binding ResourceMapping}"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Stretch"
                                    Background="{StaticResource SurfaceBrush}">
                <syncfusion:SfScheduler.ResourceHeaderTemplate>
                    <DataTemplate>
                        <StackPanel Margin="8,0,0,0">
                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="14" Foreground="#2C3E50"/>
                            <TextBlock Text="{Binding Type}" FontSize="12" Foreground="#7B8A8B"/>
                        </StackPanel>
                    </DataTemplate>
                </syncfusion:SfScheduler.ResourceHeaderTemplate>
            </syncfusion:SfScheduler>
        </Border>
    </Grid>
</UserControl>
