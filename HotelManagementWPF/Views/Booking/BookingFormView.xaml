<Window x:Class="HotelManagementWPF.Views.Booking.BookingFormView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Book Room" Height="540" Width="480" WindowStartupLocation="CenterOwner" ResizeMode="NoResize" Background="#FAFAFA">
    <Window.Resources>
        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#2196F3"/>
            <Setter Property="Margin" Value="0,16,0,8"/>
        </Style>
        <Style x:Key="FormLabel" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Margin" Value="0,8,0,2"/>
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,4"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
        </Style>
        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#E3F0FF"/>
            <Setter Property="Foreground" Value="#2196F3"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,4"/>
            <Setter Property="Margin" Value="0,0,0,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
        </Style>
    </Window.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="32">
            <!-- Guest Information -->
            <TextBlock Text="Guest Information" Style="{StaticResource SectionHeader}"/>
            <TextBlock Text="Full Name" Style="{StaticResource FormLabel}"/>
            <TextBox Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="Phone Number" Style="{StaticResource FormLabel}"/>
            <TextBox Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="Email Address" Style="{StaticResource FormLabel}"/>
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="Age" Style="{StaticResource FormLabel}"/>
            <TextBox Text="{Binding Age, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="Gender" Style="{StaticResource FormLabel}"/>
            <ComboBox SelectedItem="{Binding Gender}" ItemsSource="{Binding GenderOptions}"/>

            <!-- Payment Details -->
            <TextBlock Text="Payment Details" Style="{StaticResource SectionHeader}"/>
            <TextBlock Text="Payment Method" Style="{StaticResource FormLabel}"/>
            <ComboBox SelectedItem="{Binding PaymentMethod}" ItemsSource="{Binding PaymentMethodOptions}"/>
            <TextBlock Text="Advanced Payment" Style="{StaticResource FormLabel}"/>
            <TextBox Text="{Binding AdvancedPayment, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="Total Amount" Style="{StaticResource FormLabel}"/>
            <TextBox Text="{Binding TotalAmount, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="Payment Status" Style="{StaticResource FormLabel}"/>
            <ComboBox SelectedItem="{Binding PaymentStatus}" ItemsSource="{Binding PaymentStatusOptions}"/>

            <!-- Stay Details -->
            <TextBlock Text="Stay Details" Style="{StaticResource SectionHeader}"/>
            <TextBlock Text="Room Number" Style="{StaticResource FormLabel}"/>
            <TextBox Text="{Binding RoomNumber}" IsReadOnly="True"/>
            <TextBlock Text="Room Type" Style="{StaticResource FormLabel}"/>
            <ComboBox SelectedItem="{Binding RoomType}" ItemsSource="{Binding RoomTypeOptions}" IsReadOnly="True"/>
            <TextBlock Text="Check-in Date" Style="{StaticResource FormLabel}"/>
            <DatePicker SelectedDate="{Binding CheckInDate}"/>
            <TextBlock Text="Check-out Date" Style="{StaticResource FormLabel}"/>
            <DatePicker SelectedDate="{Binding CheckOutDate}"/>
            <TextBlock Text="Number of Nights" Style="{StaticResource FormLabel}"/>
            <TextBox Text="{Binding NumberOfNights}" IsReadOnly="True"/>

            <!-- Buttons -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,24,0,0">
                <Button Content="Cancel" Style="{StaticResource SecondaryButton}" Command="{Binding CancelCommand}"/>
                <Button Content="Book Room" Style="{StaticResource PrimaryButton}" Command="{Binding BookRoomCommand}"/>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Window>
